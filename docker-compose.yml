version: '3.8'

services:
  swarm-orchestrator:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: swarm-mcp-server
    ports:
      - "8000:8000"
    volumes:
      # Bind mount for live development
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
      # API keys (pass from host environment)
      - OPENAI_API_KEY=${OPENAI_API_KEY:-}
      - GEMINI_API_KEY=${GEMINI_API_KEY:-}
    restart: no # or "on-failure" if you want it to restart only if it fails

    networks:
      - swarm-network

networks:
  swarm-network:
    driver: bridge
