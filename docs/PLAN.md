# 🧠 Swarm Context Loader (RAM)

> **System Instruction**: This is the Root Node. Do NOT read `docs/ai/memory/*` unless triggered by the# Project Swarm: PLAN

## 🎯 Current Focus
- Establishing the **Autonomous Git Team** and unifying the **Rolling Memory** system.

## ⚡ Search Triggers (Load on Demand)
- **Big Picture**: `search_codebase("Swarm Master Plan")`
- **Workflow**: `search_codebase("Memory Lifecycle Workflow")`
- **Debugging**: `search_codebase("Swarm Error Log")`
- **Memory Skills**: `search_codebase("Memory Skills")`

## 🔮 Next Steps
1. Configure `GitWorker` to detect local changes.
2. Use `Memory Refresh Skill` to consolidate today's logs into a daily summary.
3. Verify `HippoRAG` can map dependencies between memory files.
